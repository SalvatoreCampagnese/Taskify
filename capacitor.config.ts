import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'it.scampagnese.taskify',
  appName: 'taskify',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
